# ??????7000
# server.servlet.context-path:?????????URL
server:
  port: 7000
  servlet:
    context-path: /ad-sponsor


# spring.jpa.hibernate.ddl-auto: ??Hibernate?????????????????????????????????
# ddl: Data Definition Language: none, create, create-drop, update, validate
spring:
  application:
    name: eureka-client-ad-sponsor
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate.format_sql: true
    # ??Open-in-View??, ??????????????????
    open-in-view: false
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/imooc_ad_data?autoReconnect=true
    username: root
    password: Djangobbs
    tomcat:
      # Spring Boot??Tomcat???????????
      max-active: 4
      # Spring Boot?Tomcat?????????????
      min-idle: 2
      # Spring Boot?Tomcat?????????
      initial-size: 2

eureka:
  client:
    service-url:
      defaultZone: http://server1:8000/eureka/

